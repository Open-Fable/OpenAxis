# Start Development

Start the OpenHub development environment:

1. Check that all 3 apps are cloned in `apps/`
2. Run `npm run dev` to start Electron + all services
3. Report the status of each service (port, health check)

If any app is missing, run `bash scripts/setup.sh` first.
